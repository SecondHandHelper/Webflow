{
  "name": "webflow",
  "version": "1.0.0",
  "description": "",
  "browserslist": ["> 0.5%", "last 2 versions", "not dead"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clean && parcel itemConfirmation.js sellItem.js itemValuation.js itemPage.js editItem.js --hmr-port 50619",
    "clean": "rm -rf .parcel-cache dist",
    "build": "npm run clean && npx parcel build itemConfirmation.js && npx parcel build sellItem.js && npx parcel build itemValuation.js && npx parcel build itemPage.js && npx parcel build editItem.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SecondHandHelper/Webflow.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/SecondHandHelper/Webflow/issues"
  },
  "homepage": "https://github.com/SecondHandHelper/Webflow#readme",
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "dependencies": {
  }
}
